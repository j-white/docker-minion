postgres:
  image: postgres:9.5.1
opennms:
  build: opennms
#  image: opennms/opennms
  ports:
   - "18101:8101"
   - "8980:8980"
  # Work around for too many files issue when running the Liquibase migrations
  privileged: true
  ulimits:
    nofile:
      soft: 40000
      hard: 80000
  links:
   - postgres
minion:
  build: minion
#  image: opennms/minion
  ports:
   - "28101:8101"
  links:
   - opennms
